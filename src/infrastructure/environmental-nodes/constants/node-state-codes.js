/* ====================================================================
   NODE STATE CODES
   Everything the node reports autonomously — no system command needed.
   ==================================================================== */

/* Connectivity (100–199) */
const NODE_STATE_CONNECTED = 100;
const NODE_STATE_DISCONNECTED = 101;
const NODE_STATE_HEARTBEAT = 102;
const NODE_STATE_UNKNOWN = 103;
const NODE_STATE_OFF = 104;

/* Session Lifecycle (200–299) */
const NODE_STATE_LOGIN_ATTEMPT = 200;
const NODE_STATE_LOGGED_IN = 201;
const NODE_STATE_LOGGED_OUT = 202;
const NODE_STATE_AUTH_FAILED = 203;

/* Operational (300–399) */
const NODE_STATE_ACTIVATED = 300;
const NODE_STATE_IDLE = 301;
const NODE_STATE_INACTIVE = 302;
const NODE_STATE_PENDING = 303;
const NODE_STATE_DISABLED = 304;
const NODE_STATE_UPDATING = 305;
const NODE_STATE_MAINTENANCE = 306;
const NODE_STATE_STREAMING_SENSORS = 307;

/* Alerts and Warnings (400–499) */
const NODE_STATE_ALERT = 400;
const NODE_STATE_URGENT = 401;
const NODE_STATE_BATTERY_LOW = 402;
const NODE_STATE_MAINTENANCE_REQUIRED = 403;

/* Errors and Failures (500–599) */
const NODE_STATE_ERROR = 500;
const NODE_STATE_SENSOR_FAILURE = 501;
const NODE_STATE_MEASUREMENT_ERROR = 502;
const NODE_STATE_SENSOR_TIMEOUT = 503;
const NODE_STATE_SENSOR_OUT_OF_RANGE = 504;
const NODE_STATE_SENSOR_DATA_CORRUPTED = 505;
const NODE_STATE_SENSOR_DATA_TX_FAILED = 506;
const NODE_STATE_PAYLOAD_FORMAT_INVALID = 507;
const NODE_STATE_POWER_FAILURE = 508;
const NODE_STATE_MEMORY_OVERFLOW = 509;
const NODE_STATE_INVALID_CONFIGURATION = 510;
const NODE_STATE_METADATA_TX_FAILED = 511;
const NODE_STATE_MQTT_DISCONNECTED = 512;

/* Provisioning and Lifecycle (600–699) */
const NODE_STATE_NEWLY_REGISTERED = 600;
const NODE_STATE_CONFIGURING = 601;
const NODE_STATE_AUTHENTICATED_PERM = 602;
const NODE_STATE_READY_TO_ACTIVATE = 603;
const NODE_STATE_DECOMMISSIONED = 604;

export {
    NODE_STATE_CONNECTED,
    NODE_STATE_DISCONNECTED,
    NODE_STATE_HEARTBEAT,
    NODE_STATE_UNKNOWN,
    NODE_STATE_OFF,
    NODE_STATE_LOGIN_ATTEMPT,
    NODE_STATE_LOGGED_IN,
    NODE_STATE_LOGGED_OUT,
    NODE_STATE_AUTH_FAILED,
    NODE_STATE_ACTIVATED,
    NODE_STATE_IDLE,
    NODE_STATE_INACTIVE,
    NODE_STATE_PENDING,
    NODE_STATE_DISABLED,
    NODE_STATE_UPDATING,
    NODE_STATE_MAINTENANCE,
    NODE_STATE_STREAMING_SENSORS,
    NODE_STATE_ALERT,
    NODE_STATE_URGENT,
    NODE_STATE_BATTERY_LOW,
    NODE_STATE_MAINTENANCE_REQUIRED,
    NODE_STATE_ERROR,
    NODE_STATE_SENSOR_FAILURE,
    NODE_STATE_MEASUREMENT_ERROR,
    NODE_STATE_SENSOR_TIMEOUT,
    NODE_STATE_SENSOR_OUT_OF_RANGE,
    NODE_STATE_SENSOR_DATA_CORRUPTED,
    NODE_STATE_SENSOR_DATA_TX_FAILED,
    NODE_STATE_PAYLOAD_FORMAT_INVALID,
    NODE_STATE_POWER_FAILURE,
    NODE_STATE_MEMORY_OVERFLOW,
    NODE_STATE_INVALID_CONFIGURATION,
    NODE_STATE_METADATA_TX_FAILED,
    NODE_STATE_MQTT_DISCONNECTED,
    NODE_STATE_NEWLY_REGISTERED,
    NODE_STATE_CONFIGURING,
    NODE_STATE_AUTHENTICATED_PERM,
    NODE_STATE_READY_TO_ACTIVATE,
    NODE_STATE_DECOMMISSIONED,
};
