const clientMqtt = 'disconnected';
const host = 'disconnected';
const protocol = 'disconnected';
const subscribe = 'disconnected';
const port = 0;
const sensors = 0;
const clientId = 0;
const connected = false;

export { clientMqtt, sensors, host, port, protocol, clientId, subscribe, connected };
