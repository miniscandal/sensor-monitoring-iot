const button = 'radio_button_unchecked';
const statusBroker = 'broadcast_on_home';
const connectedSensors = 'settings_remote';
const host = 'dns';
const port = 'route';
const protocol = 'nat';
const clientId = 'api';
const sensorActive = 'motion_sensor_active';
const sensors = 'sensors';
const modeOffOn = 'mode_off_on';
const humidityPorcentage = 'humidity_percentage';
const temperaturePorcentage = 'device_thermostat';
const terminal = 'terminal';
const logs = 'storage';
const topicSubscribe = 'rate_review';

/* not used */
const sensorUrgent = 'motion_sensor_active';
const sensorAlert = 'motion_sensor_active';
const sensorsOff = 'sensors_off';

export {
    button,
    statusBroker,
    connectedSensors,
    host,
    port,
    protocol,
    clientId,
    sensorActive,
    sensorUrgent,
    sensorAlert,
    sensorsOff,
    sensors,
    modeOffOn,
    humidityPorcentage,
    temperaturePorcentage,
    terminal,
    logs,
    topicSubscribe,
};
